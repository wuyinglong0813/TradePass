<view class="es {{compact ? 'es-compact' : ''}}">
  <image class="es-img" src="/images/mascot.png" mode="aspectFit"/>
  <view class="es-text">{{text}}</view>
  <button wx:if="{{buttonText}}" class="es-btn" bindtap="onTap">{{buttonText}}</button>
</view>
